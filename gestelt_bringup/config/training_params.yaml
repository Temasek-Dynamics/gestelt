training:
  TRAIN_FROM_CHECKPOINT: false
  PDP_GRADIENT: true
  USE_PREV_SOLVER: false
  num_epochs: 200
  # steps_per_epoch: 20
  close_loop_horizon: 3
  learning_rate: 0.0001
  dyn_decay: 0.7
  batch_size: 1 
