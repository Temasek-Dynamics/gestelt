from setuptools import setup, find_packages

setup(
    name='nn_policy',
    version='0.1.0',
    packages=find_packages(),#['modules'],  # <-- This should match your Python package name
    install_requires=[],
    zip_safe=True,
)
